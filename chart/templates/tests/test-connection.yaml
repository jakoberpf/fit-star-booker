apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fit-star-booker.fullname" . }}-test-connection"
  labels:
    {{- include "fit-star-booker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fit-star-booker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
