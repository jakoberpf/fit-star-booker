apiVersion: v1
kind: Service
metadata:
  name: {{ include "fit-star-booker.fullname" . }}
  labels:
    {{- include "fit-star-booker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fit-star-booker.selectorLabels" . | nindent 4 }}
